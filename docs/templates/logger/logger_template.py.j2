# File: app/logger/logger_config.py
# AUTO-GENERATED BY CODEX
"""
Logging Initialisierung auf Basis von config/config_logging.json
"""

import logging.config
import json
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent.parent
DEFAULT_CONFIG = BASE_DIR / "config" / "config_logging.json"


def init_logging(config_path: str = str(DEFAULT_CONFIG)) -> None:
    """Initialisiert das globale Logging gemäß JSON-Konfiguration."""
    with open(config_path, "r", encoding="utf-8") as f:
        config = json.load(f)
        logging.config.dictConfig(config)
